name: dispatch test
on: push
#on:
#  workflow_dispatch:
#    inputs:
#      x:
#        description: manual override for x
#        type: choice
#        required: true
#        options:
#          - 'a'
#          - 'b'
#          - 'c'

jobs:
  my_job:
    strategy:
      matrix:
        x:
          - a
          - b
          - c
    uses: ./.github/workflows/reusable.yaml
    with:
      x: ${{ matrix.x }}
